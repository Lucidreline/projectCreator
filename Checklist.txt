    Run app from terminal
X    back up into the 'projects' dir
X    take in argument from terminal (new dir/project name)
X    return error to console if project name already exists (echo "error already exists")
X    Create a new dir in the 'projects' dir
X    go into the newly created dir
X    create CheckList.txt, README.md, app.py
X    Navigate to github and create a repo
X    git init, git add ., git commit, git remote add origin (git remote add origin git@github.com:Lucidreline/X.git), pit push
X    open dir with visual studio code!

    